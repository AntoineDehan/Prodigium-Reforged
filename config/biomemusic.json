{
  "delayModifier": {
    // Modifies the delay between songs(requires a game restart), lower = shorter delay (1.0 = vanilla). 
    // Default = 0.25
    "delayModifier": 0.35
  },

  "pitchVariance": {
    // Adds randomized pitch variance of up to the given value (e.g. 0.1), makes songs sound slightly different.
    // Default = 0
    "pitchVariance": 0.0
  },

  "musicVariance": {
    // Enables more varied music to be available for biomes chosen from similar other biomes, 
    // Default = true
    "musicVariance": true
  },

  "smartMusic": {
    // Enables smart stopping of music, when the biome changes or player leaves a cave/water/night is over, 
    // Default = true
    "smartMusic": false
  },

  "stopMusicForRecords": {
    // Enables Jukeboxes and records playing to stop the current background music, 
    // Default = true
    "stopMusicForRecords": true
  },

  "musicConditions": {
    // Sets the condition under which music from their respective categories is allowed to play. Required conditions need to be all met,
    // a single matched disallowed conditions prevents it. Possible conditions are: [NETHER, OVERWORLD, END,
    // WATER, CAVE, OTHERDIMENSIONS, NIGHT]
    "Biome": {
      "requires": "[]",

      "disallowed": "[]"
    },

    "Cave": {
      "requires": "[CAVE]",

      "disallowed": "[WATER, NETHER, END]"
    },

    "Night": {
      "requires": "[NIGHT]",

      "disallowed": "[CAVE, WATER, END, NETHER]"
    },

    "Water": {
      "requires": "[WATER]",

      "disallowed": "[END, NETHER]"
    },

    "Game": {
      "requires": "[]",

      "disallowed": "[WATER, NETHER, END]"
    },

    "End": {
      "requires": "[END]",

      "disallowed": "[]"
    },

    "Nether": {
      "requires": "[NETHER]",

      "disallowed": "[]"
    }
  },

  "displayMusicPlayed": {
    // Shows currently played music in chat/log, 
    // Default = false
    "displayMusicPlayed": false
  },

  "logloadedmusic": {
    // Prints the music files getting loaded into the latest.log, 
    // Default = false
    "logloadedmusic": false
  },

  "maxConcurrentSounds": {
    // Set the maximum amount of times the same sound can play at the same time. Limits the amount of lag spamming sounds can create.
    // Default = 10
    "maxConcurrentSounds": 10
  }
}
