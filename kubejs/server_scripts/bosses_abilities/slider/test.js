// EntityEvents.hurt((event) => {
//   const entity = event.entity;
//   const player = event.player;

//   // Affiche un message chaque fois que l'événement est déclenché
//   if (!entity) return;

//   player.tell("You hit something!");
// });
